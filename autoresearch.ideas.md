- Replace the monolithic `BasiliskII/src/uae_cpu_2021/compiler/compemu_support.cpp` path for `CPU_aarch64` with an arm-specific support unit derived from Amiberry's `src/jit/arm/compemu_support_arm.cpp`. The current shim approach keeps uncovering large sets of x86-only helper assumptions (`mov_b_rm`, `and_l`, `raw_fp_init`, stack-layout macros, `cache_tags`/`mem_banks`/`jit_exception` plumbing), suggesting the architecture split is the cleaner path.
- Before retrying the arm-specific support split, add a very small Basilisk AArch64 compatibility layer shared by `compemu.h`, `compemu_midfunc_arm64*.cpp`, and the imported support unit: define the minimal `currprefs`/`changed_prefs` surface, expose `flags_carry_inverted` + `clobber_flags`/`readreg` declarations to `codegen_arm64.cpp`, and decide whether `jit_exception`/`cache_tags`/`mem_banks` live in `regstruct` or as globals.
- If the arm-specific support split is resumed, reconcile the ABI in one focused pass instead of ad-hoc shims: map `countdown`/`NATMEM_OFFSET`/`baseaddr`/`special_mem`/`jit_n_addr_unsafe`, add the AArch64 `popall_*_setpc` symbols, and either extend Basilisk's `regstruct` to match the imported ARM JIT expectations or patch the imported support file to Basilisk's actual `fp_result`/scratch-reg layout.
- After the imported ARM support path reaches link, add a Basilisk-facing wrapper/export layer instead of renaming the imported functions: export `flush_icache`, `jit_abort`, `compiler_use_jit`, `m68k_compile_execute`, and overload/adapt generated entry points like `readbyte/writebyte/readword/writeword/readlong/writelong/calc_disp_ea_020` to the exact signatures expected by `compemu.cpp`.
- The current import front is no longer header syntax; the next focused compatibility batch should be Basilisk runtime helpers and UAE naming aliases: `uae_bswap_16/32`, `CYCLE_UNIT`, `do_extra_cycles`, `set_special`, `ua`/`xfree`, NOFLAGS-table handling (`nfctbl`/`nfcpufunctbl`), and the imported FPU/regstruct expectations (`regs.fp[i].fp`, untranslated-opcount storage) before spending more time on wrapper polish.
- Once that helper batch is in, the remaining blockers look like a small final-link wrapper surface again: define a real Basilisk-owned `canbang` symbol (not a macro), ensure the imported ARM `flush_icache` function-pointer variable is actually defined, and add/repair the exact-signature wrappers for `register_branch` plus any still-missing `prepare_for_call_*` entry points.
